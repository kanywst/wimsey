//! `wimsey-mtls` — the WIMSE mutual TLS transport binding.
//!
//! Target spec: `draft-ietf-wimse-mutual-tls-01`. The client certificate is a
//! WIC (Workload Identity Certificate); identity is bound to the TLS session.
//!
//! Phase 3 — implementation pending. See `ROADMAP.md`.
