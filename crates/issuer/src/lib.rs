//! `wimsey-issuer` — an experimental WIMSE credential issuer.
//!
//! Scoped as a reference/experimentation issuer (a Rust counterpart to Cofide's
//! `minispire`), not a SPIRE replacement. Exposes a SPIFFE Workload API shim so
//! workloads can obtain WITs while interoperating with the SPIFFE ecosystem.
//!
//! Phase 4 — implementation pending. See `ROADMAP.md`.
