//! `wimsey-httpsig` — the WIMSE HTTP Message Signatures transport binding.
//!
//! Target spec: `draft-ietf-wimse-http-signature-03`, a profile of RFC 9421.
//! Carries the WIT and WPT and signs the covered HTTP components so an
//! intermediary can read but not tamper with the request.
//!
//! Phase 3 — implementation pending. See `ROADMAP.md`.
